apiVersion: v1
kind: Service
metadata:
  name: book-api-service
spec:
  selector:
    app: book-api
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080
      nodePort: 30080
  type: NodePort
